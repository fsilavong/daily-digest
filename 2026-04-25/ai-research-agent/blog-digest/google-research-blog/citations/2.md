# Citation 2

Source: https://research.google/blog/reasoningbank-enabling-agents-to-learn-from-experience/

> ReasoningBank is a novel agent memory framework that uses successful and failed experiences to distill generalizable reasoning strategies, enabling an agent to continuously learn from experience after deployment.
>
> We introduce a novel agent memory framework ... that distills useful insights from both successful and failed experiences for test-time self-evolution. When evaluated on web browsing and software engineering benchmarks, ReasoningBank enhances both agent effectiveness (higher success rates) and efficiency (fewer task steps) compared to baseline approaches.
>
> Each structured memory item contains the following: Title ... Description ... Content ... The memory workflow operates in a continuous, closed loop of retrieval, extraction, and consolidation.
>
> Crucially, unlike existing workflow memory strategies that only focus on successful runs, ReasoningBank actively analyzes failed experiences to source counterfactual signals and pitfalls.
>
> We bridge this gap by explicitly linking memory with scaling through memory-aware test-time scaling (MaTTS). ... Parallel scaling ... Sequential scaling ...
>
> From our main evaluation results with Gemini-2.5-Flash on WebArena and SWE-Bench-Verified ... ReasoningBank without scaling outperformed memory-free agents by 8.3% on WebArena and 4.6% on SWE-Bench-Verified. ... ReasoningBank w/ MaTTS improves over ReasoningBank by a 3% success rate increase and 0.4 fewer steps on WebArena.
>
> In a web-browsing example, the agent's initial curated rules resembled simple procedural checklists ... As the agent persisted through more problem sets, these memories were incorporated during execution.